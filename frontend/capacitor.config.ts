import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.eata.tuganado',
  appName: 'Tu ganado',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
